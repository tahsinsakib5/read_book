import 'package:flutter/material.dart';

class Secodhomepage extends StatelessWidget {
  const Secodhomepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
      
            Text("home")
          ],
        ),
      ),
    );
  }
}